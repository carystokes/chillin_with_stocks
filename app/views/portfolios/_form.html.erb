<%= form_for @portfolio do |f| %>
  <%= f.label :title, "Title" %>
  <%= f.text_field :title %>
  <%= f.label :cash, "Cash" %>
  <%= f.number_field :cash %>
  <%= f.submit "Create Portfolio" %>
<% end %>
